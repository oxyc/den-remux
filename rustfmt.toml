# The same settings as den-atlas and den-reel, so every Rust addon formats alike.
edition = "2021"
max_width = 110

# Keep short structs, calls and matches on one line instead of exploding them one field per line.
use_small_heuristics = "Max"
